<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="x-apple-disable-message-reformatting">
  <title>{{subject}}</title>
  <style>
    :root {
      color-scheme: light dark;
      supported-color-schemes: light dark;
    }

    body,
    .email-bg {
      margin: 0 !important;
      padding: 0 !important;
      background: {{backgroundColor}} !important;
      font-family: "Avenir Next", "Segoe UI", "Helvetica Neue", Arial, sans-serif !important;
      -webkit-text-size-adjust: 100%;
      -ms-text-size-adjust: 100%;
    }

    table {
      border-collapse: collapse;
      border-spacing: 0;
    }

    img {
      border: 0;
      display: block;
      line-height: 100%;
      -ms-interpolation-mode: bicubic;
    }

    .canvas {
      width: 100%;
      max-width: 680px;
      margin: 0 auto;
      background: #ffffff;
    }

    .kicker {
      margin: 0;
      font-size: 11px;
      line-height: 1.4;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: {{accentColor}};
      font-weight: 700;
    }

    .headline {
      margin: 8px 0 0;
      font-size: 40px;
      line-height: 1.1;
      font-weight: 760;
      letter-spacing: -0.03em;
      color: #0f172a;
    }

    .subhead {
      margin: 14px 0 0;
      font-size: 18px;
      line-height: 1.66;
      color: #334155;
      font-weight: 460;
    }

    .copy {
      margin: 0;
      font-size: 16px;
      line-height: 1.9;
      color: #1f2937;
      white-space: pre-line;
    }

    .cta {
      display: inline-block;
      padding: 14px 28px;
      border-radius: 2px;
      font-size: 15px;
      line-height: 1;
      font-weight: 700;
      letter-spacing: 0.01em;
      text-decoration: none;
      color: {{buttonTextColor}} !important;
      background: {{buttonColor}};
      animation: ctaPulse 2.8s ease-in-out infinite;
    }

    .toggle-input {
      display: none !important;
      max-height: 0 !important;
      overflow: hidden !important;
      visibility: hidden !important;
    }

    .toggle-label {
      display: inline-block;
      padding: 10px 14px;
      border: 1px solid #d4d8df;
      border-radius: 2px;
      color: #334155;
      font-size: 12px;
      line-height: 1;
      letter-spacing: 0.02em;
      font-weight: 700;
      cursor: pointer;
      user-select: none;
    }

    .toggle-panel {
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      transition: max-height 0.35s ease, opacity 0.25s ease;
    }

    #bm-toggle:checked + .toggle-label + .toggle-panel {
      max-height: 260px !important;
      opacity: 1 !important;
    }

    .footer {
      margin: 0;
      font-size: 12px;
      line-height: 1.75;
      color: #64748b;
      white-space: pre-line;
    }

    @keyframes ctaPulse {
      0%,
      100% { transform: translateY(0); }
      50% { transform: translateY(-1px); }
    }

    @media screen and (max-width: 680px) {
      .canvas {
        width: 100% !important;
      }

      .headline {
        font-size: 34px !important;
        font-size: clamp(30px, 8vw, 38px) !important;
      }

      .subhead {
        font-size: 17px !important;
      }

      .copy {
        font-size: 15px !important;
        line-height: 1.82 !important;
      }

      .stack-pad {
        padding-left: 20px !important;
        padding-right: 20px !important;
      }
    }

    @media (prefers-color-scheme: dark) {
      body,
      .email-bg {
        background: #05070c !important;
      }

      .canvas {
        background: #0b1220 !important;
      }

      .line {
        border-color: #223045 !important;
      }

      .headline {
        color: #f8fafc !important;
      }

      .subhead,
      .copy {
        color: #d1d5db !important;
      }

      .footer {
        color: #93a5bf !important;
      }

      .toggle-label {
        border-color: #334155 !important;
        color: #e2e8f0 !important;
        background: #111827 !important;
      }

      .hero-image {
        filter: brightness(0.88) saturate(1.06);
      }
    }

    [data-ogsc] body,
    [data-ogsc] .email-bg {
      background: #05070c !important;
    }

    [data-ogsc] .canvas {
      background: #0b1220 !important;
    }

    [data-ogsc] .line {
      border-color: #223045 !important;
    }

    [data-ogsc] .headline {
      color: #f8fafc !important;
    }

    [data-ogsc] .subhead,
    [data-ogsc] .copy {
      color: #d1d5db !important;
    }

    [data-ogsc] .footer {
      color: #93a5bf !important;
    }

    @supports (font-variation-settings: normal) {
      .headline {
        font-variation-settings: "wght" 760;
      }

      .subhead {
        font-variation-settings: "wght" 480;
      }

      .copy {
        font-variation-settings: "wght" 430;
      }
    }
  </style>
</head>
<body class="email-bg" style="margin:0;padding:0;background:{{backgroundColor}};font-family:'Avenir Next','Segoe UI','Helvetica Neue',Arial,sans-serif;">
  <div style="display:none;max-height:0;overflow:hidden;opacity:0;color:transparent;mso-hide:all;">{{preheader}}</div>

  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" class="email-bg" style="background:{{backgroundColor}};margin:0;padding:0;">
    <tr>
      <td align="center" style="padding:0 10px;">
        <table role="presentation" width="680" cellpadding="0" cellspacing="0" border="0" class="canvas" style="width:100%;max-width:680px;background:#ffffff;">
          <tr>
            <td style="height:10px;background:linear-gradient(90deg,{{accentColor}} 0%,{{buttonColor}} 100%);"></td>
          </tr>

          <tr>
            <td style="padding:0;">
              <img src="{{image}}" alt="{{title}}" width="680" class="hero-image" style="display:block;width:100%;max-width:680px;height:auto;min-height:280px;object-fit:cover;border:0;">
            </td>
          </tr>

          <tr>
            <td class="stack-pad line" style="padding:36px 34px 16px;border-bottom:1px solid #e6eaf0;">
              <p class="kicker" style="margin:0;font-size:11px;line-height:1.4;letter-spacing:0.14em;text-transform:uppercase;color:{{accentColor}};font-weight:700;">{{templateName}}</p>
              <h1 class="headline" style="margin:8px 0 0;font-size:40px;line-height:1.1;font-weight:760;letter-spacing:-0.03em;color:#0f172a;">{{title}}</h1>
              <p class="subhead" style="margin:14px 0 0;font-size:18px;line-height:1.66;color:#334155;">{{subtitle}}</p>
            </td>
          </tr>

          <tr>
            <td class="stack-pad" style="padding:24px 34px 8px;">
              <p class="copy" style="margin:0;font-size:16px;line-height:1.9;color:#1f2937;white-space:pre-line;">{{content}}</p>
            </td>
          </tr>

          <tr>
            <td class="stack-pad" style="padding:14px 34px 24px;">
              <a href="#" class="cta" style="display:inline-block;padding:14px 28px;border-radius:2px;font-size:15px;line-height:1;font-weight:700;text-decoration:none;color:{{buttonTextColor}};background:{{buttonColor}};">{{cta}}</a>
            </td>
          </tr>

          <tr>
            <td class="stack-pad" style="padding:0 34px 24px;">
              <input id="bm-toggle" class="toggle-input" type="checkbox">
              <label for="bm-toggle" class="toggle-label" style="display:inline-block;padding:10px 14px;border:1px solid #d4d8df;border-radius:2px;color:#334155;font-size:12px;line-height:1;font-weight:700;letter-spacing:0.02em;cursor:pointer;">Open quick highlights</label>
              <div class="toggle-panel" style="max-height:0;overflow:hidden;opacity:0;">
                <p style="margin:12px 0 0;font-size:13px;line-height:1.72;color:#475569;">
                  This newsletter uses dark-mode adaptation, fluid responsive typography, and a kinetic disclosure block for clients that support advanced CSS.
                </p>
              </div>
            </td>
          </tr>

          <tr>
            <td class="stack-pad line" style="padding:22px 34px 34px;border-top:1px solid #e6eaf0;">
              <p class="footer" style="margin:0;font-size:12px;line-height:1.75;color:#64748b;white-space:pre-line;">{{footer}}</p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
