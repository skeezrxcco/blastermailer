# Bugs

## AI Chat

- [ ] when the user clicks on the eye icon to preview the template, the template is automatically selected, a template should be selected only if the user clicks on the selected action button
- [ ] if a user comes scrolls up and select a different template, then the new template should be selected and the previous selected template should be removed from the chat
- [ ] the template cards are huge reduce the buttons realestate and spacing between the buttonsnad the cards content.the eye icon should be replaced by a preview action button
- [ ] should be able to edit messages
- [ ] the template editor AI chat should be able to edit the template
- [ ] the mode select is being duplicated theres an UI problem
- [ ] the mails are not being sent
- [ ] for the sending of emails if the user bought an email account in the platform then the user should be able to send emails using that account otherwise the platform will use the default platform email account
- [ ] the chat should ask if the user wants to schedule a campaign or a one timesend immediately, if the user chooses to schedule then the chat should ask for the date and time using a date picker (and a recurring option)
- [ ] immediately sending should be done in background and the user should be able to see the progress of the sending. either by navigation to the activity page or an indicator on the current chat conversation.
- [ ] the chat should show the progress of the sending of the email
- [ ] the user will be free to continue using the same chat conversation while the email is being sent or start a new conversation
- [ ] chats are not being persisted
- [ ] the user should be able to see the history of the chat conversation on click the chat conversation on the sidebar
- [ ] the user should be able to delete any chat conversation
- [ ] templates should be generated on the fly if not matched any template
- [ ] Free tier users can only send a max of 100 emails per month
